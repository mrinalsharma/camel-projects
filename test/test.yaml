apiVersion: camel.apache.org/v1
kind: Integration
metadata:
  name: test
spec:
  flows:
    - route:
        from:
          uri: kamelet:timer-source
          steps:
            - log:
                message: hello world
          parameters:
            period: 1000
