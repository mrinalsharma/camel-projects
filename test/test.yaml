apiVersion: camel.apache.org/v1
kind: Integration
metadata:
  name: test
spec:
  flows:
    - route:
        from:
          uri: kamelet:webhook-source
          steps:
            - to:
                uri: direct
            - to:
                uri: log
          parameters:
            subpath: /test
